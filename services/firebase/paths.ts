/** Firestore path segments — single source of truth for Phase 1+ repositories. */
export const FS = {
  users: 'users',
  trades: 'trades',
  alerts: 'alerts',
  journal: 'journal',
  notifications: 'notifications',
} as const;
